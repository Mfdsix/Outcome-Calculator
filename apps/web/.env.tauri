# Native Tauri shell (desktop / Android / iOS) — loaded only via `--mode tauri`.
# The browser PWA build ignores this file and keeps hitting same-origin /api.
# Replace with the LAN IP (or tunneled hostname) of the API server:
VITE_API_URL=http://192.168.1.x:3000
